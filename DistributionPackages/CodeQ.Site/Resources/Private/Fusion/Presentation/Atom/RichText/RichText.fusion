prototype(CodeQ.Site:Presentation.Atom.RichText) < prototype(Neos.Fusion:Component) {
    @styleguide {
        props {
            content = CodeQ.Site:Presentation.Atom.RichText.ContentFixture
        }
    }

    @propTypes {
        @strict = true
        class = ${PropTypes.string}
        content = ${PropTypes.string}
    }

    class = null
    content = null

    renderer = afx`
        <div class={String.trim('prose ' + props.class)}>
            {props.content}
        </div>
    `
}
