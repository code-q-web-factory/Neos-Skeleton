##
# This renders the background preview image,
# which is used in eager loaded Image.LazyPicture and can be used
# for a preload tag
##
prototype(CodeQ.Site:Component.Image.BackgroundPreviewImage) < prototype(Neos.Neos:ImageUri) {
	# api
	asset = false

	# rendering
	@if.enabled = ${this.asset}
	maximumWidth = 100
}

